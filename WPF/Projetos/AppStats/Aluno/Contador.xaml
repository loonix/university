<UserControl x:Class="AppStatistics.Contador"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Orientation="Horizontal" Margin="15 0 0 15">
            <Border Width="50" Height="50" Background="#FFF1F1" CornerRadius="10">
                <TextBlock Text="&#xe712;" Foreground="#FF7261" FontFamily="/fonts/#iconfont"  FontSize="25"
                                           VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
            <StackPanel Margin="15 0 0 0">
                <TextBlock Text="{Binding Titulo}" Foreground="#3F4C5D"/>
                <TextBlock Text="{Binding Valor}" FontWeight="Bold" Foreground="#3F4C5D" FontSize="28"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
